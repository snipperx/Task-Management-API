meta {
  name: Delete Project
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/projects/{{projectId}}
  body: none
  auth: inherit
}

docs {
  409 Conflict if the project still has active (non-Done) tasks.
}
