meta {
  name: Assign Role
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/users/{{userId}}/roles
  body: json
  auth: inherit
}

body:json {
  {
    "role": "Developer"
  }
}

docs {
  `role`: Viewer | Developer | Manager | Admin. Needs `users:manage` (Admin).
}
